"""
Provider module - API client wrappers for external ML services.

Currently, API providers are implemented directly in the analyzer files.
This module is reserved for shared client utilities if needed in the future.
"""
