# Terraform Variables Configuration Example
# Copy this file to terraform.tfvars and fill in your actual values
# terraform.tfvars is gitignored for security

# Required: Your Linode API token
# Get it from: https://cloud.linode.com/profile/tokens
linode_token = "your-linode-api-token-here"

# Optional: Override default region (default: es-mad - Madrid)
# Available regions: eu-central, us-east, us-central, us-southeast, etc.
region = "es-mad"

# Optional: Override domain name (default: mydomain.example)
# domain_name = "mydomain.example"

# Optional: Override domain email (default: admin@mydomain.example)
# domain_email = "admin@mydomain.example"

# Optional: Override SSH public key path (default: ~/.ssh/id_rsa.pub)
# ssh_public_key_path = "~/.ssh/id_rsa.pub"

# Optional: Override instance type (default: g6-standard-2)
# Available types: g6-nanode-1, g6-standard-1, g6-standard-2, g6-standard-4, etc.
# instance_type = "g6-standard-2"

# Optional: New Relic license key for infrastructure monitoring
# new_relic_license_key = "your-new-relic-license-key"

# Optional: New Relic account ID for infrastructure monitoring
# new_relic_account_id = "your-new-relic-account-id"

# Optional: Object Storage region (default: es-mad - Madrid)
# Available regions: es-mad, eu-central, us-east, etc.
# object_storage_region = "es-mad"

# Optional: Object Storage bucket label (default: telegram-media)
# object_storage_bucket_label = "telegram-media"

# Optional: Object Storage lifecycle expiration in days (default: 365)
# object_storage_lifecycle_expiration_days = 365

# Optional: Object Storage noncurrent version expiration in days (default: 5)
# object_storage_noncurrent_version_expiration_days = 5

